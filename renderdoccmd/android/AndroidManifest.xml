<?xml version="1.0"?>
<!-- BEGIN_INCLUDE(manifest) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="@RENDERDOC_ANDROID_PACKAGE_NAME@">

  <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="26"/>

  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- gpu layer adb命令shell settings需要的权限-->
  <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

  <!-- Use GL ES 3.2 version -->
  <uses-feature android:glEsVersion="0x00030000" android:required="true" />

  <application 
	android:debuggable="true" 
	android:label="@APK_LABEL_NAME@" 
	android:icon="@drawable/icon" 
	android:hasCode="true"
  android:requestLegacyExternalStorage="true"
	>
    <activity android:name="@RENDERDOC_ANDROID_PACKAGE_NAME@.Loader" android:label="@APK_LABEL_NAME@" android:exported="true" android:screenOrientation="landscape" android:configChanges="orientation|keyboardHidden">
		<intent-filter>
			<action android:name="android.intent.action.MAIN" />
			<category android:name="android.intent.category.LAUNCHER" />
		</intent-filter>
      <meta-data android:name="android.app.lib_name" android:value="renderdoccmd"/>
    </activity>

  </application>
</manifest>
<!-- END_INCLUDE(manifest) -->
